

## Change "Strategy" to "Approach"

A simple text update in `src/pages/AboutUs.tsx`.

### What changes
- **File**: `src/pages/AboutUs.tsx`, line 23
- **Change**: Update the `title` property from `'Strategy'` to `'Approach'` in the values array

This is a one-line change that will update the heading displayed in the Mission/Vision/Strategy section.

